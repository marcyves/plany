<h1><%= taskLabel %> Edition</h1>

<ul class="list-group">
    <li class="list-group-item active" aria-current="true" ><%= task.taskName %> of <%= project.name %></li>
    <li class="list-group-item">Steps: <%= task.steps %></li>
    <% for( let index = 1; index <= task.steps; index++ ) { %>
      <li class="list-group-item"> #<%= index %> Date: <input type="date"> Duration:<input type="number" size="2" step="0.1" id="quantity" name="quantity" min="1" max="5"></li>    
    <% } %>
  </ul>